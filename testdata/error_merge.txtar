merge function: should error because CUE uses unification, not mutable merging.

-- input.yaml --
data: {{ merge .Values.a .Values.b }}
-- error --
"merge" has no CUE equivalent: CUE uses unification instead of mutable map merging
