max/min in pipeline position: piped value becomes last argument.
Helm's max/min do not support piped input, so no Helm comparison.

-- values.yaml --
port: 8080
-- input.yaml --
apiVersion: v1
kind: ConfigMap
metadata:
  name: test
data:
  port: {{ .Values.port | max 1024 }}
  capped: {{ .Values.port | min 65535 }}
-- output.cue --
import "list"

#values: {
	port!: bool | number | string | null
	...
}

apiVersion: "v1"
kind:       "ConfigMap"
metadata: name: "test"
data: {
	port: list.Max([1024, #values.port])
	capped: list.Min([65535, #values.port])
}
