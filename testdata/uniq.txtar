uniq function: remove duplicate elements from a list.
No semantic comparison: Helm renders Go slice format [a b c] while CUE
produces a proper list.

-- values.yaml --
items:
  - apple
  - banana
  - apple
  - cherry
  - banana
-- input.yaml --
apiVersion: v1
kind: ConfigMap
metadata:
  name: test
data:
  unique: {{ .Values.items | uniq }}
-- output.cue --
import "list"

#values: {
	items!: _
	...
}

apiVersion: "v1"
kind:       "ConfigMap"
metadata: name: "test"
data: unique: (_uniq & {#in: #values.items}).out
_uniq: {
	#in: [...]
	out: [for i, x in #in if !list.Contains(list.Slice(#in, 0, i), x) {x}]
}
